# TimeJump - Project Context for AI Assistant

**ملاحظة للـ AI (أنا في المستقبل):** 
هذا الملف يحتوي على ملخص كامل لمشروع `TimeJump` (injection-LSPosed) لتجنب إعادة شرح المشروع من الصفر في حال بدأ المستخدم محادثة جديدة. يرجى قراءة هذا الملف كاملاً قبل إجراء أي تعديلات.

---

## 📌 معلومات أساسية (Credentials & Repo)
- **GitHub Repository:** `ghjfyu9t8fg/injection-LSPosed`
- **GitHub Token (PAT):** [موجود في سكربتات الرفع المحلية الخاصة بالمطور]
- **آلية الرفع (Upload & Build):** نقوم بتعديل الملفات محلياً، ثم نستخدم `Invoke-RestMethod` في الـ PowerShell لرفع الملفات إلى GitHub، ثم نستدعي `build.yml` (Workflow Dispatch) لعمل Build للـ APK.

---

## 🚀 المعمارية الحالية للمشروع (وصلنا للإصدار v4.0)
البرنامج عبارة عن أداة أتمتة شاملة تتكون من جزئين أساسيين:

### 1. LSPosed Module (`MainHook.java`)
- **وظيفته:** يُحقن داخل اللعبة المستهدفة (مثل Free Fire: `com.dts.freefireth`).
- **Time Hooking:** يقوم بخداع وظائف الوقت في الجافا (مثل `System.currentTimeMillis`).
- **Lua Engine:** يقوم بحقن مكتبة `libTimeJump.so` وتشغيل سكريبت `best.lua` للتحكم في اللعبة من الداخل.
- **التشغيل التلقائي للخدمة:** بمجرد أن تفتح اللعبة (عن طريق `Activity.onResume`)، يقوم `MainHook` بإرسال `Intent` لتشغيل `AutoClickerService` الخاصة بنا في الخلفية تلقائياً.

### 2. Standalone Foreground Service (`AutoClickerService.java`)
- **وظيفته:** يعمل كخدمة مستقلة تماماً عن اللعبة باستخدام صلاحيات الروت (لا توجد مشاكل مع حماية الألعاب لأنه يضغط من الخارج).
- **أخذ لقطات الشاشة:** يستخدم `su -c screencap`.
- **الضغط:** يستخدم `su -c input tap`.
- **البحث عن الصور (Smart Macro):** إضافة أهداف (صور مقصوصة) ليقوم البرنامج بالبحث عنها في الشاشة والضغط عليها، وذلك عبر **Strict Sequence State Machine** (يبحث عن الهدف الأول، ثم بعد الضغط عليه يبحث عن الثاني، وهكذا في سلسلة منتظمة).
  - **Smart Mode:** حفظ الإحداثيات بعد أول نقرة لتسريع العملية وعدم إرهاق المعالج (Caching).
  - **Click Limits:** تحديد عدد مرات الضغط على الهدف (0 = لا نهائي).
  - **Delays:** التحكم في السرعة بين النقرات (`global_click_delay`) والانتقال بين الأهداف (`global_step_delay`).
  - **Paste Text (Dynamic URL):** إمكانية لصق نص معين (أو إحضار نص ديناميكي من رابط مباشر مثل GitHub Gist/Pastebin) بعد النقر على هدف محدد مع تأخير زمني مخصص (Paste Delay) باستخدام `su -c input text`.

### 3. Auto-Boot Game (`BootReceiver.java`)
- **وظيفته:** جعل البرنامج يعمل بدون أي تدخل بشري.
- عند إقلاع النظام (المحاكي/الموبايل)، يلتقط `BootReceiver` الحدث، ينتظر 10 ثوانٍ، ثم يفتح اللعبة المحددة مسبقاً باستخدام `su -c monkey`. مما يؤدي بدوره إلى تفعيل `MainHook` ثم `AutoClicker` ثم `best.lua`.

### 4. واجهة المستخدم (`MainActivity.java` & `activity_main.xml`)
- تم تقسيم الواجهة إلى 3 تابات (Tabs):
  1. **Script:** لكتابة وحفظ كود Lua.
  2. **Macro:** لإضافة صور الأهداف (Targets)، وتعديل إعدادات كل هدف (Smart Mode / Click Limits)، والتحكم في السرعات.
  3. **Boot:** لاختيار اللعبة التي ستعمل تلقائياً مع إقلاع النظام.

---

## 🛠️ كيف تساعد المستخدم عند العودة؟
1. **لا تشرح الأساسيات:** المستخدم مبرمج متقدم ومدرك تماماً لطبيعة الروت، و LSPosed، وكيفية عمل النظام.
2. **الهدف القادم:** بعد قراءة هذا الملف، اسأل المستخدم: "قرأت السياق بالكامل، إيه التعديل أو الميزة اللي هنضيفها دلوقتي؟ هل نبدأ في كتابة سكريبت اللعبة `best.lua` ولا نعدل حاجة في الـ UI/Service؟"
