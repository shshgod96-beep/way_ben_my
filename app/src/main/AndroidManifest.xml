<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.timejump.lsposed">

    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.QUERY_ALL_PACKAGES" />
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.TimeJump">
        
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@android:style/Theme.DeviceDefault">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Auto Clicker Foreground Service -->
        <service
            android:name=".AutoClickerService"
            android:exported="true" />
            
        <service
            android:name=".FloatingCoordService"
            android:exported="false" />
            
        <!-- Boot Receiver for Auto-Start feature -->
        <receiver
            android:name=".BootReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.QUICKBOOT_POWERON" />
            </intent-filter>
        </receiver>
        
        <!-- Declare as an Xposed Module -->
        <meta-data
            android:name="xposedmodule"
            android:value="true" />
            
        <!-- Module Description -->
        <meta-data
            android:name="xposeddescription"
            android:value="Dynamically injects Lua scripts into Free Fire." />
            
        <!-- Minimum Xposed Version required -->
        <meta-data
            android:name="xposedminversion"
            android:value="93" />
            
        <!-- Define module scope (Games to target by default in LSPosed) -->
        <meta-data
            android:name="xposedscope"
            android:resource="@array/xposed_scope" />
            
    </application>
</manifest>
